// Content below is autogenerated by ccsmodule engine
// usually there is no reason to edit it manually
goog.provide('<%-name%>');

goog.require('cssmodule.Stylesheet');

/**
 * @constructor
 * @extends {cssmodule.Stylesheet}
 */
<%-name%> = function() {
  goog.base(this, '<%-name%>',
      ['<%-data.replace(/\n*\/\*@ sourceMappingURL=.+? \*\/\n*$/, '').split('\n').map(function(line) {
        return line.replace(/'/g, "\\'");
      }).join("\\n','")%>'].join(''),
      '<%-uri%>');
};
goog.inherits(<%-name%>, cssmodule.Stylesheet);

/** @return {<%-name%>} */
<%-name%>.install = function() {
  var style = new <%-name%>();
  style.install();
  return style;
};

<%-name%>.uninstall = function() {
  cssmodule.Manager.getInstance().uninstall('<%-name%>');
};
